from setuptools import setup

setup(
    name = 'rate_studies',
    version = '0.0.1',
    packages = ['rate_studies'],
    install_requires = [
        'numpy',
        'scipy',
        'root_numpy'
        ]
)
